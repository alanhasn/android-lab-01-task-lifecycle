<?xml version="1.0" encoding="utf-8"?>
<!-- Root manifest tag defines the package name and XML namespaces -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Application configuration including backup, icons, and theme -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTaskFlowLab">

        <!-- Main entry point activity. Exported must be true to be launchable -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidTaskFlowLab">
            <intent-filter>
                <!-- Specifies this activity as the main entry point -->
                <action android:name="android.intent.action.MAIN" />
                <!-- Specifies this activity should appear in the launcher -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Second activity with singleInstance launch mode to demonstrate task behavior -->
        <activity
            android:name=".SecondActivity"
            android:launchMode="singleInstance"/>

        <!-- Third activity with standard launch mode -->
        <activity android:name=".ThirdActivity" />

    </application>

    <!-- Permissions required by the application -->
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>
